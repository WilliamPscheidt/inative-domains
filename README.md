Script simples feito para me auxiliar no meu trabalho diário.

Esse script detecta domínios que não possuem mais configuração DNS atrelada e exclui a tabela DNS do servidor, para que não acomule milhares de tabelas dns inutilizadas.
